#!/usr/bin/env zsh

alias awkcsv='awk -F "\"*,\"*"'

alias cx='chmod +x'

alias ls="ls -GF"
alias la="ls -A"
alias ll="ls -l"

# allow exiting with C-c
alias sl="sl -e"

alias mkdir="mkdir -pv"

alias please=sudo
